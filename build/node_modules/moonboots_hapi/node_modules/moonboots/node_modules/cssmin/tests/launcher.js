// division-by-zero-test.js

var vows = require('vows'),
    assert = require('assert'),
	fs = require('fs'),
	util = require('util');

 fs.readdir("/Users/johan/Work/cssmin/tests/data", function(err, list) {
    list.forEach(function(file) {
		util.puts(file);
    });
  });
